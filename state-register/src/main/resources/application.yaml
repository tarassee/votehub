server:
  port: 8081

spring:
  application:
    name: state-register-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/state_register
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

rsa:
  key:
    bitsize: 16
